# 报表导出算法设计

1. 解析日期参数（输入：date，输出：day）
2. 校验导出权限（输入：user，输出：allowed，备注：需人工确认代码是否实现）
3. 按日期读取报表数据（输入：day，输出：base）
4. 记录导出指标（输入：day）
5. 计算报表总额（输入：base，输出：computed，备注：系数 1.15 需核对）
6. 渲染输出报表（输入：computed、format，输出：report）
